{
  "name": "tutorials-account-balances",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "/usr/local/bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "viktor-kovtun-biz",
  "license": "MIT",
  "devDependencies": {
    "@polkadot/api": "^6",
    "@subql/cli": "latest",
    "@subql/types": "latest",
    "typescript": "^4.5.2"
  }
}
